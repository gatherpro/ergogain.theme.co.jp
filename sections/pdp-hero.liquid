{% comment %}
  PDP Hero - LCP最適化全幅ヒーロー

  使い方：
  1. 管理画面 > オンラインストア > テーマ > カスタマイズ
  2. 「セクションを追加」> 「PDP Hero」を選択
  3. テキスト、メディア、CTAを設定

  機能：
  - LCP最適化（fetchpriority="high", eager loading）
  - 2カラムレイアウト（左：テキスト｜右：メディア）
  - 画像/動画切り替え対応
  - 2つのCTA（購入/カスタム開始）
  - レスポンシブ対応
  - アクセシビリティ対応
{% endcomment %}

<link rel="stylesheet" href="{{ 'pdp-shared.css' | asset_url }}" media="print" onload="this.media='all'">
<noscript><link rel="stylesheet" href="{{ 'pdp-shared.css' | asset_url }}"></noscript>

<style>
  .pdp-hero-{{ section.id }} {
    background-color: {{ section.settings.bg }};
    color: {{ section.settings.fg }};
    padding-top: {{ section.settings.padding_y }}px;
    padding-bottom: {{ section.settings.padding_y }}px;
  }

  .pdp-hero-{{ section.id }} .pdp-hero__inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    align-items: center;
  }

  @media screen and (min-width: 990px) {
    .pdp-hero-{{ section.id }} .pdp-hero__inner {
      grid-template-columns: 1fr 1fr;
      gap: 6rem;
      padding: 0 4rem;
    }
  }

  .pdp-hero-{{ section.id }} .pdp-hero__heading {
    font-size: {{ section.settings.title_size }}px;
    line-height: {{ section.settings.title_lh }};
    color: {{ section.settings.fg }};
  }

  @media screen and (max-width: 749px) {
    .pdp-hero-{{ section.id }} .pdp-hero__heading {
      font-size: calc({{ section.settings.title_size }}px * 0.7);
    }
  }

  .pdp-hero-{{ section.id }} .pdp-hero__btn--primary {
    background-color: {{ section.settings.fg }};
    color: #ffffff;
  }

  .pdp-hero-{{ section.id }} .pdp-hero__btn--primary:hover {
    background-color: #424245;
  }

  .pdp-hero-{{ section.id }} .pdp-hero__btn--ghost {
    color: {{ section.settings.fg }};
    border-bottom: 1px solid {{ section.settings.fg }};
  }
</style>

<section
  class="pdp-hero pdp-hero-{{ section.id }}"
  role="region"
  aria-label="{{ section.settings.heading | escape }}"
>
  <div class="pdp-hero__inner">
    <div class="pdp-hero__copy">
      {%- if section.settings.kicker != blank -%}
        <p class="pdp-hero__kicker">{{ section.settings.kicker }}</p>
      {%- endif -%}

      {%- if section.settings.heading != blank -%}
        <h1 class="pdp-hero__heading">{{ section.settings.heading }}</h1>
      {%- endif -%}

      {%- if section.settings.subtext != blank -%}
        <p class="pdp-hero__subtext">{{ section.settings.subtext }}</p>
      {%- endif -%}

      {%- if section.settings.primary_label != blank and section.settings.primary_link != blank -%}
        <div class="pdp-hero__ctas">
          <a
            href="{{ section.settings.primary_link }}"
            class="pdp-hero__btn pdp-hero__btn--primary"
            aria-label="{{ section.settings.primary_label | escape }}"
          >
            {{ section.settings.primary_label }}
          </a>

          {%- if section.settings.secondary_label != blank and section.settings.secondary_link != blank -%}
            <a
              href="{{ section.settings.secondary_link }}"
              class="pdp-hero__btn pdp-hero__btn--ghost"
              aria-label="{{ section.settings.secondary_label | escape }}"
            >
              {{ section.settings.secondary_label }}
            </a>
          {%- endif -%}
        </div>
      {%- endif -%}
    </div>

    <div class="pdp-hero__media">
      {%- if section.settings.media_type == 'video' and section.settings.video != blank -%}
        <video
          class="pdp-hero__video"
          playsinline
          autoplay
          muted
          loop
          {% if section.settings.poster != blank %}
            poster="{{ section.settings.poster | image_url: width: 1600 }}"
          {% endif %}
          width="1600"
          height="1200"
          aria-label="製品動画"
        >
          <source src="{{ section.settings.video }}" type="video/mp4">
          お使いのブラウザは動画タグをサポートしていません。
        </video>
      {%- elsif section.settings.image != blank -%}
        {%- assign image_height = section.settings.image.width | divided_by: section.settings.image.aspect_ratio | round -%}
        <img
          class="pdp-hero__image"
          srcset="
            {%- if section.settings.image.width >= 600 -%}{{ section.settings.image | image_url: width: 600 }} 600w,{%- endif -%}
            {%- if section.settings.image.width >= 900 -%}{{ section.settings.image | image_url: width: 900 }} 900w,{%- endif -%}
            {%- if section.settings.image.width >= 1200 -%}{{ section.settings.image | image_url: width: 1200 }} 1200w,{%- endif -%}
            {%- if section.settings.image.width >= 1600 -%}{{ section.settings.image | image_url: width: 1600 }} 1600w,{%- endif -%}
            {%- if section.settings.image.width >= 2000 -%}{{ section.settings.image | image_url: width: 2000 }} 2000w{%- endif -%}
          "
          src="{{ section.settings.image | image_url: width: 1200 }}"
          sizes="(min-width: 990px) 50vw, 100vw"
          alt="{{ section.settings.image.alt | default: section.settings.heading | escape }}"
          width="{{ section.settings.image.width }}"
          height="{{ image_height }}"
          fetchpriority="high"
          loading="eager"
        >
      {%- else -%}
        <div class="pdp-hero__placeholder">
          {{ 'product-1' | placeholder_svg_tag: 'pdp-hero__placeholder-svg' }}
        </div>
      {%- endif -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "PDP Hero",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "コンテンツ"
    },
    {
      "type": "text",
      "id": "kicker",
      "label": "キッカー（任意）",
      "info": "見出し上の小さなテキスト"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "見出し",
      "default": "Premium Quality. Designed for You.",
      "info": "短く、インパクトのある見出しにしてください"
    },
    {
      "type": "textarea",
      "id": "subtext",
      "label": "説明文",
      "default": "Experience the perfect blend of comfort and performance. Designed with precision, crafted for excellence.",
      "info": "80-140文字推奨"
    },
    {
      "type": "header",
      "content": "メディア"
    },
    {
      "type": "select",
      "id": "media_type",
      "label": "メディアタイプ",
      "options": [
        {
          "value": "image",
          "label": "画像"
        },
        {
          "value": "video",
          "label": "動画"
        }
      ],
      "default": "image"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "画像",
      "info": "推奨サイズ: 1600x1200px以上"
    },
    {
      "type": "text",
      "id": "video",
      "label": "動画URL (.mp4)",
      "info": "MP4ファイルの完全なURLを入力"
    },
    {
      "type": "image_picker",
      "id": "poster",
      "label": "動画ポスター画像",
      "info": "動画読み込み前に表示されるサムネイル"
    },
    {
      "type": "select",
      "id": "object_fit",
      "label": "メディアの表示方法",
      "options": [
        {
          "value": "cover",
          "label": "カバー（スペースを埋める）"
        },
        {
          "value": "contain",
          "label": "コンテイン（全体を表示）"
        }
      ],
      "default": "cover"
    },
    {
      "type": "header",
      "content": "CTA（行動喚起）"
    },
    {
      "type": "text",
      "id": "primary_label",
      "label": "メインボタンラベル",
      "default": "購入"
    },
    {
      "type": "url",
      "id": "primary_link",
      "label": "メインボタンリンク"
    },
    {
      "type": "text",
      "id": "secondary_label",
      "label": "サブボタンラベル",
      "default": "カスタム開始"
    },
    {
      "type": "url",
      "id": "secondary_link",
      "label": "サブボタンリンク"
    },
    {
      "type": "header",
      "content": "レイアウト・スタイル"
    },
    {
      "type": "range",
      "id": "padding_y",
      "min": 40,
      "max": 160,
      "step": 10,
      "unit": "px",
      "label": "上下余白",
      "default": 80
    },
    {
      "type": "color",
      "id": "bg",
      "label": "背景色",
      "default": "#f5f5f7"
    },
    {
      "type": "color",
      "id": "fg",
      "label": "テキスト色",
      "default": "#1d1d1f"
    },
    {
      "type": "range",
      "id": "title_size",
      "min": 22,
      "max": 48,
      "step": 2,
      "unit": "px",
      "label": "見出しサイズ",
      "default": 40
    },
    {
      "type": "range",
      "id": "title_lh",
      "min": 1.1,
      "max": 1.4,
      "step": 0.05,
      "label": "見出し行間",
      "default": 1.2
    }
  ],
  "presets": [
    {
      "name": "PDP Hero"
    }
  ]
}
{% endschema %}
